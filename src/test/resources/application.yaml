spring:
  application:
    name: tontine-test
  threads:
    virtual:
      enabled: true

app:
  cors:
    allowed-origins:
      - http://localhost:4200
  jwt:
    secret: TestSecretKeyForUnitTestsOnly-Must-Be-32-Chars!!
    expiration-ms: 900000

